#include<stdio.h>


int main(){
	int x[3]={0};
	int *p= NULL;
	int *tmp;
	int i;

	for(p = x;p<x+3;p++){
		scanf("%d",p);
	}

	for(i=0;i<2;i++){
		for(p = x;p<x+2;p++){
			if(*(p)>=*(p+1)){
				*tmp = *p;
				*p = *(p+1);
				*(p+1) = *tmp;
			}
		}
	}

	printf("%d\n",*(x+1));

	return 0;
}

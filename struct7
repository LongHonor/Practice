#include <stdio.h>

typedef struct number{
	int INT;
	int rank;
}rank; 

int main(){
	int i = 0,j=0,cnt = 0;
	rank rk[10] = {0,0};
	
	for(i=0;i<10;i++){
		scanf("%d",&rk[i].INT);
	}
	
	for(i = 0;i<10;i++){
		cnt = 0;
		for(j=0;j<10;j++){
			if(rk[i].INT>rk[j].INT){
				cnt++; 
			}
		}
		rk[i].rank = cnt;
	}
	
	for(i= 0; i<10;i++){
		if(rk[i].rank == 7)
		printf("%d ",rk[i].INT);
	}
	for(i= 0; i<10;i++){
		if(rk[i].rank == 3)
		printf("%d ",rk[i].INT);
	}
	
	return 0;
}
